# Asset Provenance

Lantern 7 separates source licensing from media asset rights.

## MIT-covered material

The repository's source code and documentation are licensed under the MIT
License unless a file has a more specific notice.

## Reserved media assets

The following directories contain AI-generated or project-specific media assets
and are not covered by the MIT code license:

- `public/art/`
- `public/audio/`

These assets were generated for Lantern 7 through local generation
scripts and AI media tooling, including OpenRouter-backed image/audio workflows.
They are included so the live game, local development build, recruiter demo,
and screenshots work out of the box.

The committed media set is intentionally lean:

- Runtime art includes briefing/result images, map thumbnails, ability icons,
  Warden rank crests, freeplay rival portraits, menu background art, and lore
  fragment illustrations still referenced from `src/game/lore.ts`.
- Enemy portraits generated by older art passes are not committed; enemies are
  rendered on canvas by the game.
- Runtime audio includes music packs, sector ambience, stingers, the briefing,
  boss/menu tracks, and dynamic announcer/ability voice lines.
- Archive text remains in source, but the old `frag-read-*.mp3` narration files
  were removed while the Archive UI is absent.

`tests/unit/audio-assets.test.ts` guards the dynamically constructed audio paths
so future cleanup does not accidentally create silent 404s.

Do not redistribute, relicense, sell, or reuse the media assets outside this
project without separate permission from the project owner.

## Committed demo/admin data

`public/balance-report.json` is intentionally tracked. It is generated by the
headless balance harness and gives the local admin dashboard useful demo data
without requiring private production telemetry.
